#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main- Prints all possible ckmbinations of letter 0-9 separted by , the space. no printf no puts
 * Return: Always 0 {Success)
 *
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
	{
	putchar(i);
	if (i != '9')
	}
	putchar(',');
	putchar(' ');
	}
	}
	putchar('\n');
	return (0);
}
