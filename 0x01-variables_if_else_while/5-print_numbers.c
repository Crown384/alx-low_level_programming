#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main- Prints each single unit of base 1
 * Return: Always 0 success
 *
 */

int main(void)
{
	int len;

	for (len = 0; len <= 9; len++)
	printf("%d", len);
	printf("\n");
	return (0);
}

